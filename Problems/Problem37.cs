using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Problems
{
    class Problem37 : BaseProblem
    {
        public override int ProblemId
        {
            get { return 37; }
        }

        public override string DoProblem()
        {
            var count = 0;
            var sum = 0;
            while (count < 11)
            {
                // find out if number is truncatable
            }
            return sum.ToString();
        }
    }
}
